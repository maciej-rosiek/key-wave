This folder is the AppExtension PublicFolder declared in Package.appxmanifest.
The Windows Dynamic Lighting host doesn't read anything from here — it just
needs the folder to exist so the manifest is valid.
